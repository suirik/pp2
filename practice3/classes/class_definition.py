# Defining a simple class
class Room:
    # A simple attribute
    name = "Living Room"

# Creating an object (instance) of the class
my_room = Room()

# Accessing the attribute
print(f"The name of the room is: {my_room.name}")